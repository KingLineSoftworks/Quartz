name: Quartz Linux Unit Tests

on:
  push:
    branches:
      - "ci_ssh"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Hello world
        run: echo "Hello world"

